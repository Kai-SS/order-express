import classes from "./ProductsSummary.module.css";

const ProductsSummary = () => {
  return (
    <section className={classes.summary}>
      <h2>Supports Hybird Team</h2>
      <p>
        Enable your remote workforce with the one-stop bulk ordering system.
      </p>
      <p> Distributing company assets easily and fast.</p>
    </section>
  );
};

export default ProductsSummary;
